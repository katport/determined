# model.fit(x_train, y_train, epochs=20, batch_size=256, validation_data=(x_test, y_test))
description: lambda
hyperparameters:
  global_batch_size: 256
searcher:
  name: single
  metric: val_loss
  max_steps: 5
entrypoint: model_def:LambdaTrial
max_restarts: 0
batches_per_step: 235
min_validation_period: 1
